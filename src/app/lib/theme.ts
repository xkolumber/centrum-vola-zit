"use client";
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontSize: 14,
    body1: {
      fontSize: "1.4rem",
    },
  },
});

export default theme;
